/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add any other configuration options you had here
  // For example:
  // images: { domains: ['example.com'] },
  // env: { MY_VAR: process.env.MY_VAR },
};

module.exports = nextConfig;